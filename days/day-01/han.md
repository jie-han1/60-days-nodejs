第一天学习注意事项：
- process.argv 第三个参数才是有效参数
- os 是 Node.js 的内置核心模块，提供与操作系统交互的实用方法。无需安装，直接 require('os') 即可使用。
- 标准 I/O 输出 stdout 和 stdin 配套使用
- readline 是 Node.js 内置的核心模块，用于逐行读取可读流（如 process.stdin）中的输入。简单说，它让终端程序能读取用户的键盘输入。
